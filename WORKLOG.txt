================================================================
 paper-agent 작업 로그 (WORKLOG)
================================================================
이 파일은 Claude가 작업할 때마다 그때그때 갱신합니다.
세션을 새로 시작하면 이 파일을 먼저 읽고 이어서 작업합니다.
================================================================

[ 프로젝트 기본 정보 ]
- 위치: C:\Users\신원석\paper-agent
- 목적: 교육공학 논문 작성을 돕는 AI 에이전트
- 기술 스택: Python, Streamlit, Claude API (claude-sonnet-4-6)
- GitHub: https://github.com/wss2105-afk/paper-agent
- 배포 주소: https://paper-agent-production-6189.up.railway.app
- 배포: Railway (Volume /data 마운트 → PDF 영구 저장)

[ 주요 파일 ]
- app.py            : Streamlit 메인 앱 (7개 기능 모드)
- rag.py            : PDF 기반 인용 단락 작성 (BM25)
- scholar.py        : Semantic Scholar 문헌 검색
- data_analyzer.py  : 데이터 분석
- style_analyzer.py : 글쓰기 스타일 분석
- sjr_data.py       : SJR(저널 등급) 데이터
- export.py         : 내보내기 (docx 등)
- progress.md       : 기존 진행 메모(참고용)

[ 기능 모드 ]
1. 자유 질문        - Claude 자유 대화
2. 단락 작성(RAG)   - 업로드 PDF 기반 인용 단락
3. 문헌 추천        - Semantic Scholar + SSCI 우선 + SJR 등급
4. PDF 분석         - 논문 PDF 요약/분석
5. 논문 구조 설계   - 주제 → 구조 제안
6. 글쓰기 교정      - 학술체 변환
7. 참고문헌 변환    - APA 등 인용 형식

================================================================
 작업 히스토리 (최신이 위)
================================================================

---- 2026-07-21 ----
- [교정 안전장치: 숫자·기호 변조 자동 감지 경고] (사용자 "알아서 진행" 승인,
  '<' 변조 버그의 후속 안전장치)
  · 배경: 프롬프트 규칙만으로는 AI가 교정 중 숫자·기호를 바꾸는 걸 100% 못 막음.
  · _notation_drift(원문, 교정문): 숫자(소수·콤마·%)와 꺾쇠 표기(<표 1> 등)를
    Counter로 세어 사라짐/새로 생김 목록 반환 (중복 개수까지 비교).
  · 교정 결과 화면 2곳에 연결 — 텍스트 교정: 전체 비교 경고,
    원본 서식 유지: 문단별 비교("문단 N — 사라짐 `x` · 생김 `y`", 최대 10개).
    변조 없으면 조용히 넘어감. 렌더 시점 계산이라 저장된 결과에도 소급 적용.
  · 오탐 방지 확인: 띄어쓰기/문체 변경(p<.05 → p < .05), F(1,58)=4.21,
    85.3%, 1,234 등은 경고 안 뜸.
  · 검증: 단위테스트 7종 + AppTest(변조 시 경고 표시·무변조 시 미표시) 통과.
- [★버그수정★ 글쓰기 교정 시 '<' 표기가 깨지던 문제] (사용자: "< 표시가 깨져서 나옴")
  · 원인 규명 과정: 백엔드 전 경로(diff, docx/hwpx 왕복, 번호 매핑) 단위테스트
    → 전부 정상. headless Chrome(CDP)으로 실제 렌더 DOM 확인 → 화면 표시도 정상.
    실제 API 재현 테스트로 확정: ★Claude가 교정하면서 기호를 "고침"★
    - (t<2.5) → (t=2.5)로 부등호를 등호로 변조
    - <표 1> → 표 1, <Figure 2> → 그림 2로 꺾쇠괄호 제거
  · 수정: 교정 프롬프트 2곳(원본 서식 유지 ⟦N⟧ 방식 + [교정문] 텍스트 방식)에
    기호·표기 보존 규칙 추가 — 부등호/꺾쇠괄호/통계값 변경 금지.
  · 검증: 수정 프롬프트로 실제 API 호출 → p<.05, <표 1>, <Figure 2>, t<2.5
    모두 보존(PASS). py_compile 통과.
  · 참고: headless Chrome 디버깅 시 --remote-allow-origins=* + CDP websocket
    사용 (dump-dom은 이 PC에서 stdout 출력 안 됨). websocket-client 설치함.

- [데이터 분석 업로드 홀딩: 24시간 → 무기한] (사용자: "한번 업로드한거는 새롭게
  다른거 업로드하기 전까지는 계속 유지")
  · _held_load()의 24시간 만료 판정 제거, HOLD_HOURS 상수 삭제.
  · 이제 새 파일 업로드로 교체하거나 🗑️ 지우기 전까지 계속 유지(연구 맥락 포함).
  · 안내 문구 갱신 + 업로드 경과 표시에 "N일 전" 단위 추가.
  · 프로젝트별 분리는 기존 그대로(PROJ_DIR/analysis_upload) — 프로젝트 1·2·3
    각각 따로 유지됨.
- [단락 작성(RAG) 품질/프롬프트 개선] (TODO 항목 진행)
  · rag.py search()에 per_source 인자 — 논문당 최대 N개 청크를 원문 순서
    (chunk_id)로 "(...)" 구분 병합 → 더 넓은 문맥 제공. 단락 작성은 per_source=2
    사용, 다른 호출부는 기본 1로 하위호환.
  · rag.py get_head(source) — 논문 첫 청크 앞부분(제목·저자·연도가 보통 있는
    부분) 반환.
  · write_paragraph_with_refs 프롬프트 개선:
    - [논문 첫머리] 섹션 주입 → 실제 서지 정보 확인해 APA식 (저자, 연도) 인용
      + 참고문헌 목록 APA 표기 유도 (확인 안 되면 출처명 그대로, 지어내기 금지).
    - 단락 유형별 작성 요령(_PARAGRAPH_GUIDES): 서론/이론적 배경/선행연구 검토/
      논의/결론 각각 전개 방식 지시.
    - 분량 선택(_PARAGRAPH_LENGTHS): "표준 (한 단락)" / "상세 (2~3단락)" —
      UI 셀렉트박스 추가, max_tokens 2048→4096.
    - 출처 간 관점 차이 드러내기 + 구체적 수치 활용(원문 그대로만) 지침 추가.
  · 검증: rag 단위테스트(per_source 병합·순서·top_k·get_head), py_compile,
    AppTest로 단락작성·데이터분석 모드 렌더 통과.

---- 2026-07-14 ----
- [데이터 분석 설계: 업로드·연구맥락 24시간 홀딩] 커밋 0f9c02d.
  (사용자: "한번 업로드한거는 일정시간동안 홀딩. 연구맥락도 마찬가지")
  · 문제: 화면 이동/재접속하면 file_uploader 파일과 연구 맥락 입력이 사라짐.
  · 구현: PROJ_DIR/analysis_upload/에 업로드 원본 + held.json(파일명, 저장시각,
    연구맥락) 저장. 재방문 시 24시간 안이면 자동 복원(💾 안내 + 🗑️ 지우기 버튼).
    _HeldFile(BytesIO 서브클래스, .name 부여)로 load_file에 그대로 통과.
  · 연구맥락은 text_input value로 복원, 수정 시마다 held.json 갱신.
    데이터 없이 연구맥락만 먼저 입력해도 유지. 프로젝트별 분리(PROJ_DIR 기반).
  · 같은 파일이면 rerun마다 재저장 생략(held_sig 세션 키). 새 파일 올리면 교체.
  · 검증: csv/xlsx/txt 홀딩 왕복 + 만료 판정 단위테스트, AppTest로 새 세션에서
    연구맥락 복원 확인, py_compile 통과. push 완료(Railway 재배포).
- [연구문제별 분석 바로 실행] 커밋 aa94212. (사용자: "각 연구문제별 분석방법
  제시 + 버튼 누르면 바로 분석")
  · 기존에도 제안→원클릭 실행은 있었으나 어떤 연구문제의 분석인지 연결이 없었음.
  · 실행 스펙 JSON에 "연구문제" 필드 추가(프롬프트 수정) — 연구문제 하나에
    분석 여러 개면 같은 값 반복.
  · UI: "⚡ 연구문제별 분석 바로 실행" — 연구문제 헤딩(❓)별로 버튼 그룹화.
    저장 라벨도 "[연구문제] 분석명" 형태로 기록(stats_run/save_analysis).
  · 구버전 스펙(연구문제 필드 없음)도 안전(헤딩 없이 버튼만) — 하위호환.
  · 검증: 파싱+그룹화 단위 테스트, py_compile, AppTest 렌더 통과. push 완료
    (Railway 자동 재배포).

---- 2026-07-07 ----
- [★중요 발견★ 문체 프로필/업로드가 재배포마다 사라지던 근본원인]
  · 증상: "내 논문 스타일"을 한 번 학습시켜도 배포 앱에서 프로필이 없음.
  · 원인: Railway에 영구 볼륨이 /data에 마운트돼 있지 않았음(사용자 대시보드 확인).
    → /data가 임시 디스크라 push→재배포될 때마다 업로드 PDF·style_profile.json이
      통째로 삭제됨. "한 번 학습하면 유지"가 물리적으로 불가능한 상태였음.
  · 조치(사용자 몫, 대시보드): 볼륨을 /data에 Attach + Variables에 DATA_DIR=/data 확인.
    볼륨 붙인 뒤 앱에서 논문 업로드 → "스타일 분석 시작" 1회 → 이후 영구 유지.
- [방어코드 추가] app.py DATA_DIR 결정 로직:
    DATA_DIR = os.getenv("DATA_DIR") or os.getenv("RAILWAY_VOLUME_MOUNT_PATH") or "./data"
  · 볼륨만 붙어 있으면 DATA_DIR 변수 설정을 깜빡해도 자동으로 볼륨 경로에 저장됨.
  · py_compile 통과. 커밋 cd62bea "Fallback to Railway volume mount path..." push 완료.
  · 참고: push 시 원격에 사용자 커밋(5656a1a 할루시네이션 방지, 549b8ac 단락 프롬프트
    개선)이 있어 rebase 후 push함.
- [사용자 볼륨 부착 완료 → 저장소 상태 진단 UI 추가] 커밋 72a8946.
  · 사이드바 "내 논문 스타일" 위에 "💾 저장소 상태" expander 추가.
    RAILWAY_VOLUME_MOUNT_PATH 감지해서 영구/임시 저장 여부와 실제 저장 경로 표시.
- [★진짜 근본원인 확정 & 해결 (Railway CLI로 직접 진단)★]
  · Railway CLI 설치(npm i -g @railway/cli) + railway login 후 프로젝트 링크.
    ※ paper-agent의 Railway 프로젝트명은 랜덤 자동생성 "ample-playfulness"
      (서비스명 paper-agent, 볼륨 paper-agent-volume, 프로젝트ID 1405bed1...).
  · CLI로 확인한 실제 상태:
      - 볼륨 paper-agent-volume: mountPath=/data, state=READY, 5GB. ★볼륨은 처음부터 정상★
      - RAILWAY_VOLUME_MOUNT_PATH=/data 도 정상 주입돼 있었음.
      - ★범인★ 환경변수 DATA_DIR = "./data" 로 박혀 있었음!
        → 앱이 볼륨(/data)을 두고도 임시 디스크(./data)에 저장 → 재배포마다 소실.
  · 해결: railway variables --set "DATA_DIR=/data" 로 변경 → 자동 재배포.
    ※ Git Bash가 /data를 C:/Program Files/Git/data 로 자동변환하는 버그 있어서
      MSYS_NO_PATHCONV=1 붙여서 설정해야 함(주의).
    검증: railway run printenv DATA_DIR → "/data" 확인. 새 배포 RUNNING.
  · 재발방지 코드(커밋 e5b960b): DATA_DIR보다 RAILWAY_VOLUME_MOUNT_PATH를 우선.
      DATA_DIR = os.getenv("RAILWAY_VOLUME_MOUNT_PATH") or os.getenv("DATA_DIR") or "./data"
    → 이제 DATA_DIR이 잘못 설정돼 있어도 볼륨만 붙어 있으면 볼륨에 저장됨.
  · 남은 일: 배포 앱에서 내 논문 업로드 → "스타일 분석 시작" 1회. 이후 영구 유지.
- [★신기능★ 통계 직접 실행 — 데이터 분석 설계 모드 확장] 커밋 83e5447.
  · 배경: 기존엔 분석 방법을 "제안"만 하고 실제 통계는 SPSS/R로 직접 돌려야 했음.
    사용자 요청: 앱이 통계를 직접 실행 + SEM/HLM + LCA까지.
  · 새 파일 stats_runner.py — 10종 분석 실행 함수:
      t검정(독립/대응), ANOVA+Tukey 사후, 상관(Pearson/Spearman), 카이제곱,
      신뢰도(Cronbach α), 중다회귀(OLS+표준화β), HLM(MixedLM 임의절편+ICC),
      LCA(StepMix, BIC 자동 모형선택+Entropy+프로필), SEM(semopy, lavaan식+적합도)
    각 함수는 {"tables": {이름: DataFrame}, "summary": str} 반환. 한국어 오류 안내.
  · app.py: "📈 통계 직접 실행" 섹션 추가 (정량 데이터 업로드 시).
    분석 선택 → 변수 선택 UI → 실행 → 결과표 + Claude가 APA 논문 문장 해석
    (해석은 체크박스, 결과는 session_state 유지, txt 다운로드).
  · requirements.txt: scipy, statsmodels, semopy, stepmix 추가.
  · 검증: 합성 데이터 17개 테스트 전부 통과 (Python 3.14.5, scipy 1.18,
    statsmodels 0.14.6, semopy 2.3.11, stepmix 3.0.0).
    LCA는 심어놓은 2계층 구조(120/100명)를 BIC로 정확히 복원함.
  · 주의: semopy가 sklearn/sympy 의존 → Railway 빌드 시간 다소 늘어남.
- [다집단 SEM 추가] 커밋 0cbb61f. (사용자 요청 "멀티 SEM" = multi-group)
  · run_sem_multigroup: 집단별 자유 추정 → 집단별 적합도/경로계수 표 +
    구조 경로의 집단 간 차이 z검정(critical ratio, 비표준화 B 기준).
  · UI: SEM 섹션에 "다집단 비교 (선택)" selectbox 추가.
  · 한계(요약문에 자동 명시): semopy가 등가제약 미지원 → 정식 측정동일성
    검증은 불가. 논문엔 한계 명시 or Mplus/lavaan 별도 확인 권장 문구 포함.
  · 검증: 심어둔 경로차이(남 B=0.8 vs 여 B=0.1)를 0.746/0.139로 복원, z=7.0
    유의 검출. 오류 케이스 2종 포함 총 20개 테스트 전부 통과.
- [★신기능★ 논문 프로젝트 워크스페이스 + 분석 결과 영구 저장] 커밋 37c511a.
  · 사용자 요청: (1) 데이터분석설계 화면 옮기면 결과 초기화됨 → 저장 원함
    (2) 논문 여러 편을 1,2,3 버튼으로 나눠 작업 (예: 1 ai활용, 2 교사 테크놀로지)
  · 새 파일 projects.py — 프로젝트 목록/경로/분석 히스토리 관리.
    저장 구조: /data/projects.json + /data/projects/<id>/{pdfs, reference_db, analyses.json}
  · 프로젝트별 분리: 참고문헌 PDF, RAG 인덱스, 분석 결과.
    전역 유지: 내 문체 프로필(사람에 속함). 기본 3개, 최대 6개, 이름 변경 가능.
  · 레거시 마이그레이션: 기존 /data/pdfs, /data/reference_db → 프로젝트 1로
    1회 자동 이동(.migrated 마커로 멱등). 기존 데이터 안 사라짐.
  · 분석 결과: 실행 시 자동으로 프로젝트별 저장(최근 30건). 데이터분석설계
    하단 "🗂️ 저장된 분석 결과" — 표 복원 + 해석 + txt 다운로드 + 개별 삭제.
    화면 이동·재접속·재배포에도 유지됨.
  · get_library()가 db_path 인자를 캐시 키로 사용 → 프로젝트 전환 시 라이브러리 분리.
  · 검증: projects.py 단위 테스트 14개 통과(한글 DF 왕복, 마이그레이션 멱등 포함)
    + app.py bare 모드 임포트 스모크 통과.
- [글쓰기 교정: 워드 업로드 + 수정부분 빨간 글자] 커밋 0381c3e, 71e19cc.
  · .docx/.txt 업로드 지원. 6000자 넘으면 문단 범위 슬라이더로 나눠 교정
    (한 번에 다 하면 출력 잘리는 것 방지).
  · 교정 결과 재구성: [교정문]/[수정 설명] 구조화 출력 → difflib 단어 diff →
    화면(HTML)과 Word 다운로드 모두 수정된 단어만 빨간색 표시.
    export.py에 diff_segments()/to_word_redline() 추가.
  · 검증: diff 정확성(바뀐 단어만 표시+재조립 일치) + 생성 docx를 다시 열어
    빨간 run 존재 확인. 교정 모드는 공용 채팅 루프에서 분리(자체 렌더링).
- [한글 HWPX 업로드 지원] 커밋 5ee6c5e. (사용자 요청, hwpx 타입 지정)
  · data_analyzer.extract_hwpx_text(): zip 안 Contents/section*.xml을 문서
    순서로 파싱. 런 병합, 표 안 문단 중복 방지, 엔티티 복원.
  · 적용: 글쓰기 교정 업로더(.docx/.hwpx/.txt) + 데이터 분석 인터뷰 텍스트.
  · 구형 .hwp(바이너리)는 미지원 — "한글에서 HWPX로 저장 후 업로드" 안내 표시.
  · 검증: 실제 HWPX 구조 모방 테스트(다중 섹션·표·엔티티) + 오류 분기 2종 통과.
- [구형 .hwp 업로드 + HWPX 저장(빨간 표시)] 커밋 7ecd524.
  · 배경: 사용자 "hwpx 업로드 안 됨" → 원인 추정: 실제 파일이 구형 .hwp라
    선택창에서 막혔을 가능성. .hwp 직접 파싱으로 해결.
  · .hwp 업로드: data_analyzer.extract_hwp_text() — olefile로 HWP 5.0
    바이너리 파싱(BodyText 레코드, zlib, 제어문자 표), PrvText 폴백,
    암호걸린 파일 안내. pyhwp 픽스처 실제 .hwp 5개(표·각주·머리말)로 검증.
  · HWPX 저장: export.to_hwpx_redline() — 한글이 만든 빈 문서
    (assets/hwpx_template.hwpx, hwpxlib Apache-2.0)를 템플릿으로 본문 주입.
    빨간 charPr(id=100, #C00000) 추가, secPr 보존, mimetype 무압축 선두.
    검증: XML 유효성 + 빨간 run + 자체 추출기 왕복(이스케이프 포함).
  · 교정 저장 버튼 3개: Word(.docx 빨간표시) / 한글(.hwpx 빨간표시) / txt.
  · requirements: olefile 추가. (semopy용 lxml은 py3.14 로컬 미설치 →
    python-hwpx 라이브러리 대신 자체 구현 선택한 이유)
  · ※ 생성 hwpx는 구조 검증까지 완료. 실제 한글 프로그램에서 열리는지는
    사용자 확인 필요 — 문제 있으면 보고받아 수정.
- [전체 문서 교정 + HWPX 빨간글자 버그 수정] 커밋 10b2152.
  · 사용자 피드백 (1) "수정이 일부만 된다" → 교정 범위에 "전체 문서(자동 분할)"
    추가: 문단 경계로 ~3000자 구간 분할 → 구간별 순차 교정(진행바) → 이어붙임.
    구간 잘림 감지 시 경고. 범위 선택 옵션도 유지.
  · 사용자 피드백 (2) "한글에서 빨간 글자 안 보임" → 원인: charPr id=100으로
    붙였는데 한글이 charPrIDRef를 순번으로 해석 → 참조 실패로 기본 검정 적용.
    수정: id를 기존 목록에 이어지는 연속 번호(max+1, 템플릿 기준 7)로 부여.
    ※ 한글 프로그램에서 빨간 글자 확인은 사용자 재확인 필요.
  · 검증: 분할 함수(30문단 12000자 → 5구간, 문단 유실 0) + hwpx id 연속성/
    빨간 run 참조/XML 유효성/왕복 추출 통과.
- [★원본 서식 보존 교정★] 커밋 073bcd1. (사용자: "표가 원래대로 안 됨")
  · 새 파일 inplace_redline.py — 원본 docx/hwpx 구조는 그대로 두고 문단
    텍스트만 교체하는 인플레이스 방식. 표·제목·레이아웃 전부 보존.
    - collect_docx/apply_docx: 본문+표 셀(중첩 표 포함) 문단 순회, 원래 서체
      (eastAsia 한글 글꼴 포함) 보존, 수정 단어만 빨간 run.
    - collect_hwpx/apply_hwpx: 직계 텍스트 run만 교체(표·그림 객체 run 불변),
      원래 charPrIDRef 보존, 빨간 charPr 연속 id 주입. 네임스페이스 등록으로
      원 prefix 유지. charPr 자기닫힘 형태도 처리.
    - chunk_numbered/parse_numbered: ⟦N⟧ 번호 매핑으로 Claude 왕복,
      누락 번호는 원문 유지(문서 깨짐 방지).
  · UI: docx/hwpx 업로드 시 "교정 방식" 선택 — 기본 "🧩 원본 서식 유지".
    수정 문단만 미리보기(50개 한도), 원본 서식 그대로 다운로드.
  · 검증: 18개 테스트 전부 통과 (docx 표 셀 안 빨간 run, hwpx 표 보존,
    왕복 추출, 번호 파싱 에지케이스). 구형 .hwp는 인플레이스 불가(텍스트만).
- [hwpx 글자 겹침 수정 + 참고문헌 따옴표 규칙] 커밋 aa1b327.
  · 사용자: "표는 살아있는데 글자가 겹쳐 보임" → 원인: 수정한 문단에 남은
    linesegarray(줄 배치 캐시)가 옛 좌표로 그려서 겹침. 수정 문단의
    linesegarray 제거 → 한글이 열 때 재계산. 테스트 20개 통과.
  · 사용자: "학술지 이름에 따옴표 하지 않아" → SYSTEM_PROMPT 답변원칙 +
    참고문헌 변환 프롬프트에 규칙 추가 (학술지명 따옴표 금지, 이탤릭 표기).
- [LCSM + 다집단 LCSM + 제안→실행 원클릭] 커밋 4b49f50.
  · LCSM(잠재변화점수모형): run_lcsm/run_lcsm_multigroup.
    ※ semopy ModelMeans가 잠재변수 절편(d~1)에서 크래시(start_gamma1 버그)
      → 단일지표 2시점 LCSM과 수학적으로 동치인 관측 변화점수 방식으로 구현.
      구간별 Δ평균(t, dz)·Δ분산·비례변화 β(Δ~이전시점). 다집단: Δ평균 차이
      Welch t + β 차이 z검정. 동치/한계를 요약문에 자동 명시.
    검증: 심은 β=-0.3 → -0.283 복원, 다집단 차이(0.7) 검출.
  · 제안→실행: 분석 설계 프롬프트가 [실행 스펙] JSON(method+변수)도 출력
    → 변수 검증 후 "▶️ 제안된 분석 바로 실행" 버튼 생성 → 클릭 시
    stats_runner 실행 + APA 해석 + 자동 저장. 설계 결과도 session 유지.
    method 10종 매핑(ttest_ind/rel, anova, correlation, chisquare, cronbach,
    regression, hlm, lcsm(+group), lca). 깨진 JSON은 빈 스펙 폴백.
  · 수동 실행 해석 로직을 _interp_stats 공용 헬퍼로 통합.
- [코딩북 지원] 커밋 67c7c9e. (사용자: "코딩북을 읽어야 변인이 뭔지 알지")
  · 데이터 분석 설계에 "📖 코딩북" 업로더 — xlsx/csv/docx(표 포함)/hwp/hwpx/
    pdf/txt 지원. data_analyzer.load_codebook_text().
  · PROJ_DIR/codebook.txt로 프로젝트별 영구 저장(재업로드 불필요), 삭제 버튼.
  · 주입 위치 3곳: 분석 설계 프롬프트(6000자 한도) + 실행 스펙 JSON 생성
    ("설명"에 변수 의미 반영, 인자는 실제 열 이름) + 결과 해석(3000자 한도).
  · 참고: SPSS .sav는 변수 레이블 내장 → summarize_dataframe이 이미 반영 중.
  · 검증: txt/xlsx/docx(표) 추출 테스트 통과.
- [UI 통합: 단락작성+PDF분석 합침, PDF업로드 사이드바→모드 안으로] 커밋 7b959c4.
  · 사용자 요청: 단락작성(RAG)과 PDF분석을 하나로, PDF 업로드를 좌측 사이드바가
    아니라 단락작성 모드 안에 배치.
  · 모드 통합: "📚 단락 작성 · 논문 분석" — 탭 2개(✍️ 단락 작성 / 📄 논문 PDF 분석).
    상단에 참고문헌 라이브러리(업로드+학습+목록+초기화) 배치.
  · render_library_manager() 헬퍼로 라이브러리 UI 분리(사이드바에서 이동).
  · PDF 분석: 채팅 루프 대신 인라인 렌더(자체 결과+txt 저장). 라이브러리 PDF
    선택 또는 새 업로드 둘 다 가능.
  · 사이드바엔 프로젝트 전환/기능 선택/문체 프로필만 남김.
  · 검증: 컴파일 + bare 임포트 통과. 옛 모드명 잔존 참조 정리.
- [★버그수정★ 한국어 RAG 검색이 "관련 문헌 없음" 뜨던 문제] 커밋 0065264.
  · 원인: rag.py가 공백 기준 토큰화 → 한국어 조사 차이(러닝이 vs 러닝을)로
    정확매칭 실패 → BM25 점수 0 → 결과 없음.
  · 수정: rag.tokenize() 추가 — 한국어는 글자 bigram, 영문·숫자는 단어 단위.
    _load에서 저장된 토큰 대신 원문에서 재토큰화 → 토크나이저 개선이 재색인
    없이 다음 로드에 바로 반영(_save는 documents만 저장).
  · 추가: app.py 단락작성에서 검색 결과 0이면 주제를 번역(KO<->EN)해 재검색
    → 영어 논문 + 한국어 주제 케이스 대응. 빈 결과 안내 문구도 구체화.
  · 검증: 조사 다른 한국어 질의로 관련 논문 정확 랭킹, 재로드 재토큰화 정상.
  · ※ 사용자: 기존 색인은 그대로 두고 앱만 재배포되면 자동 반영(재색인 불필요).
- [단락 작성 언어 선택(한국어/English)] 커밋 5e093ac. (사용자 요청)
  · 단락 작성 탭에 "작성 언어" 셀렉트박스 추가. write_paragraph_with_refs에
    language 인자 → 프롬프트에 언어 지시 + 참고문헌 헤딩(참고문헌/References) 분기.
- [★코드 전체 점검 & 버그 수정★] 커밋 e5467ae. (사용자: "전체적으로 점검")
  · ★크래시 버그★ app.py에 io 미import인데 docx "원본 서식 유지" 교정 경로에서
    io.BytesIO 사용 → docx 기본 교정 시 NameError. import io 추가로 수정.
  · 시작 단계 migrate_legacy를 try/except로 감쌈(이전 실패가 앱 부팅 막지 않게).
  · 점검 방법: pyflakes(전 파일 undefined name 0) + Streamlit AppTest로 8개 모드
    전부 예외 없이 렌더 확인 + 프로젝트 전환/재실행 안정성 확인 + 중복 위젯키 0.
  · 무해 확인: f-string placeholder 경고들은 여러 줄 문자열 연결 오탐.
- [★신기능★ 학술지 형식 맞추기 — 참고문헌 변환 메뉴에 통합] 커밋 da42d9a.
  · 사용자 요청: 학술지 투고규정(웹주소/파일)을 주면 원고 전체를 그 형식으로
    편집. 인용·참고문헌 + 구조·형식 + 레이아웃 전부, 파일 업로드→다운로드.
    "참고문헌 변환" 메뉴 앞에 붙여 하나로.
  · 모드명 "🔖 참고문헌 변환" → "🔖 학술지 형식 · 참고문헌 변환", 탭 2개.
  · 새 파일 journal_format.py:
    - fetch_url_text: requests로 규정 웹페이지 서버측 fetch + HTML 태그 제거.
    - build_docx_from_markdown: 마크다운(#/##/###)→docx, 레이아웃(여백·글꼴·
      크기·줄간격) 적용. 한글 eastAsia 글꼴까지 설정.
    - build_hwpx_from_markdown: 한글 템플릿 기반 hwpx 생성(내용 위주).
  · 흐름: (1)규정 등록(URL/파일)→Claude가 규칙요약+레이아웃JSON 추출,
    PROJ_DIR/journal_format.json에 프로젝트별 저장. (2)원고 업로드(docx/hwpx/txt)
    →긴 원고 분할→규칙대로 재편집(마크다운)→docx/hwpx/txt 다운로드.
  · 참고문헌 변환 탭: "등록한 학술지 형식" 옵션 추가, 인라인 렌더로 전환
    (채팅 루프에서 분리). chat-modes 목록에서 참고문헌 변환 제거.
  · 검증: journal_format 단위테스트(URL제거·docx 레이아웃/제목/한글글꼴·hwpx왕복)
    + AppTest 통합모드 렌더/회귀없음.
  · 한계: docx는 레이아웃 정확 적용, hwpx는 내용 위주. 매우 긴 원고는 구간별
    편집이라 전역 섹션 재배치는 제한적.

---- 2026-06-09 (추가 세션) ----
- [글쓰기 교정 모드에 내 문체 프로필 적용 추가]
  · 기존: 스타일 프로필은 단락 작성(RAG)/인용 삽입 2개 모드에서만 적용됐음.
  · 추가: 글쓰기 교정 모드(app.py:743~)에 "🖊️ 내 문체 반영" 체크박스 추가.
    프로필 있으면 기본 ON, build_style_instruction을 교정 프롬프트에 주입.
  · 참고: 적용에는 style_profile.json만 필요. 원본 PDF(/data/my_papers)는
    재분석용으로만 보관됨(적용 시점엔 미사용). 자동삭제 여부는 추후 결정.
- [로컬 전체 의존성 설치 완료]
  · requirements.txt 7개 미설치분 설치: streamlit, pdfplumber, rank-bm25,
    python-docx, pandas, openpyxl, pyreadstat (Python 3.14.5 base).
  · 11개 패키지 전부 import 검증 통과("ALL IMPORTS OK"). 이제 로컬 풀 실행 가능.
- [★버그 수정★ 참고문헌이 전부 안 적히는 문제]
  · 증상: 문헌 추천/참고문헌 변환에서 결과가 중간에 잘려 일부만 표시.
  · 원인: chat_with_claude()의 max_tokens=4096 한도 초과로 응답 truncation.
    (문헌 추천은 논문 최대 20편 각각 분석 → 4096토큰 쉽게 초과)
  · 수정(app.py):
      (1) chat_with_claude max_tokens 4096 → 8192 상향.
      (2) return_truncated 플래그 추가 → stop_reason=="max_tokens" 감지.
      (3) 문헌 추천 + 공용 채팅 루프에 ⚠️ 잘림 경고 st.warning 표시.
  · py_compile 통과. 호출부 4곳 모두 하위호환 확인. (아직 미커밋)

---- 2026-06-09 ----
- [문헌 추천 기능 실사용 테스트 완료]
  · 검색(Semantic Scholar) / SSCI 인용순 정렬 / Claude 분석 → 모두 정상 동작 확인.
  · ★버그 발견 & 수정★ : SJR 등급 배지가 전혀 안 붙던 문제(매칭 0/8).
    원인 3가지 — (1) 학술지명에 ", Vol.X, pp.Y" 접미사가 붙어 정확매칭 실패
                  (2) " : 부제" 가 붙어 매칭 실패
                  (3) Semantic Scholar 약어("Comput. Educ.")가 표에 없음
    수정 파일: sjr_data.py
      - _normalize(): 권/페이지/부제 노이즈 제거 후 매칭
      - _ABBREV/_ABBREV_LOOKUP: 흔한 ed-tech 학술지 약어 20여 개 별칭 추가
    결과: 실제 결과셋 기준 SJR 매칭 0/8 → 3/8 (정통 Q1 저널 모두 매칭, 오탐 0).
  · 참고: 이 PC(사용자 폴더 신원석)에는 패키지가 없어 테스트용으로
          requests/python-dotenv/anthropic/arxiv 를 base python(3.14)에 설치함.
          (streamlit 등 전체 의존성은 아직 미설치 — 로컬 풀 실행하려면 추가 설치 필요)
- [커밋 & 푸시 완료] 831bbda "Fix SJR quartile matching..." → origin/master push 완료.
  · Railway 자동 재배포 트리거됨 (배포 완료까지 1~2분 소요).
  · git 작성자 정보가 이 PC에 없어서 repo-local로 wss21/wss2105@gmail.com 설정함.
- 최근 커밋:
    75776c8 Add SJR quartile badges to paper list
    6ee5d30 Add SSCI priority mode to literature recommendation
    36f48be Recommendation: smaller headings, add journal to prompt

---- 세션 종료 시점 상태 (2026-06-09) ----
- 문헌 추천 기능: 테스트 완료 + SJR 배지 버그 수정 + 커밋/푸시/재배포까지 끝.
- git: master = origin/master (831bbda), 작업 트리 clean 예정(WORKLOG 저장분만 미커밋).
- 다음 세션은 아래 TODO부터 이어가면 됨.

================================================================
 다음 할 일 (TODO)
================================================================
- [x] 문헌 추천 기능 실사용 테스트 (2026-06-09 완료, SJR 배지 버그 수정)
- [x] 로컬 전체 의존성 설치 (2026-06-09 완료)
- [x] 참고문헌 truncation 버그 수정 (2026-06-09, max_tokens 8192 + 잘림 경고)
- [x] app.py 변경분 커밋 & push (이후 세션들에서 정리 완료)
- [x] 단락 작성(RAG) 품질/프롬프트 개선 (2026-07-21: per_source 청크 병합,
      서지 정보 기반 APA 인용, 유형별 지침, 분량 선택)
- [ ] (사용자 지시 대기 중)

================================================================
 메모 / 주의사항
================================================================
- .env 에 API 키 있음 (git 추적 안 됨 / 절대 커밋·노출 금지)
- 배포는 Railway 자동: master에 push 하면 재배포됨
================================================================
